<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:toolbar="http://schemas.android.com/apk/res-auto"
    toolbar:theme="@style/ActionBarThemeOverlay"
    toolbar:popupTheme="@style/ActionBarPopupThemeOverlay"
    android:id="@+id/toolbar_actionbar"
    android:background="@color/color_primary"
    toolbar:titleTextAppearance="@style/Theme.ActionBar.TitleTextStyle"
    toolbar:subtitleTextAppearance="@style/Theme.ActionBar.SubtitleTextStyle"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toolbar_height"/>